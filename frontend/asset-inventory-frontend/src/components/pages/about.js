import React from 'react';
import { Link } from 'react-router-dom';

export default function() {
    return (
    <div>
        About
        {/* <Link to='/signup'>Sign Up</Link>
        <Link to='/auth'>Login</Link> 
        ******Use these links inside the About content.*****
        */}
    </div>
    );
}